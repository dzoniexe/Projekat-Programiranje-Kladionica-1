#ifndef LIGA_HPP_INCLUDED
#define LIGA_HPP_INCLUDED

#include "sport.hpp"
#include "tim.hpp"
#include "menadzer.hpp"

using namespace std;

/*class Liga
{
private:
    string ime;
    string drzava;
    Tim t;
public:
    Liga(string lime, string ldrzava, sport ts, string time, string tdrzava, string tmesto, Menadzer tmenadzer, int tratingHome, int tratingAway):t( ts, time, tdrzava, tmesto, tmenadzer, tratingHome, tratingAway){
       ime = lime;
       drzava = ldrzava;
    }
    };*/

#endif // LIGA_HPP_INCLUDED
